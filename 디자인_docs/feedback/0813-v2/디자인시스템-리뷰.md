# 디자인 피드백 v2

[Variables 컬렉션](https://www.figma.com/design/WfW1Nkx1oiOWBHNwrw48IL/Design-Library?node-id=126-1092) v03 · 2026-08-13
코드는 v03에 전부 맞췄습니다. 아래는 값을 바꿔달라는 게 아니라 확인이 필요한 항목입니다.
위쪽일수록 오류에 가깝고, 아래로 갈수록 판단이 필요한 항목입니다.

v03은 값 변경 0건, 이름과 구조만 바뀐 업데이트입니다. 리네임 3 + 신설 6.

## A. 오류로 보이는 것

- [ ] 1. content/rank/king, expert, sprout — 등급명 글자 색인데 흰 배경 대비가 2.02 / 2.56 / 1.92 (rookie만 4.79 통과) → 글자 기준 4.5 미달. 아래 2번과 같은 항목이라 함께 봐주세요
- [ ] 2. [badge/reporter-rank](https://www.figma.com/design/WfW1Nkx1oiOWBHNwrw48IL/Design-Library?node-id=671-9967)의 글자와 심볼이 원래 다른 색이었습니다 — 심볼 SVG는 expert #10B972(green/500), king #FFA132(orange/400)로 v03 rank 토큰과 정확히 같은데, 글자는 green/700, orange/700로 더 진했습니다. 코드가 글자를 심볼에 맞추면서 한 배지 안의 두 색이 일치하게 됐고, 그 결과 글자 대비가 낮아졌습니다 → 선택지는 셋입니다. 심볼도 같이 진하게 해서 대비 확보 / 심볼은 두고 글자만 진하게(한 배지 두 색을 허용) / 지금처럼 둘 다 밝게 유지. 토큰 값만 green/700으로 되돌리면 글자만 진해지고 심볼은 그대로라 다시 어긋납니다
- [ ] 3. content/rank/rookie, sprout — 값이 각각 content/secondary(gray/600), content/disabled(gray/400)와 완전히 동일 → 같은 값 다른 이름 2쌍입니다. 코드가 실제로 그 두 범용 토큰을 쓰다가 전용 토큰으로 갈아탔는데 렌더 변화가 0이었습니다. 특히 sprout가 disabled와 같으면 새싹이 비활성으로 읽힙니다
- [ ] 4. content/error ↔ content/accent/favorite — 둘 다 red/500으로 완전히 동일 → 에러는 글자라 4.5가 필요하고 찜 하트는 아이콘이라 3이면 됩니다. 기준이 다른데 값이 같아서 한쪽만 진하게 못 바꿉니다. 지금 갈라둘지

## B. 빠진 것

- [ ] 5. 에러 계열 — content/error 하나뿐 → 입력창 오류 테두리(border/error), 에러 안내 배경(surface/error). 지금은 테두리에 content/error를 그대로 쓰고 있습니다
- [ ] 6. 상태 색 — error만 있고 성공·경고 없음 → content/success, content/warning. 제보 완료·가격 급등 안내에 필요합니다
- [ ] 7. surface/accent 하위 — 색상 축을 열었는데 orange 2개뿐 → 다른 색이 추가될 예정이면 언제인지, 아니면 축 없이 되돌릴지
- [ ] 8. content/rank 하위 — 글자 색 4개뿐 → 등급 배지의 배경·테두리 색 (배지 형태가 확정되면)
- [ ] 9. 영업 상태 색 — 영업중은 content/brand-light를 쓰는데 영업종료에 맞는 토큰이 없음 → 에러도 찜도 아니라서 코드에 red/500이 그대로 남아 있습니다

## C. 대비 미달

코드는 Figma 값 그대로 두었습니다. 임의로 진하게 바꾸지 않았습니다.

- [ ] 10. content/error — 흰 배경 3.61 → 기준 4.5. 에러 문구는 반드시 읽혀야 하는 글자입니다. red/700이면 6.01로 통과
- [ ] 11. content/accent/badge — 흰 배경 4.45 → 기준 4.5. v02부터 이어지는 항목이라 그대로 두었습니다
- [ ] 12. content/accent/favorite — 흰 배경 3.61 → 찜 하트는 아이콘이라 기준 3을 충족합니다. 다만 같은 색을 글자에 쓰면 미달이라 용도를 아이콘으로 못박을지

## D. 이름·구조

- [ ] 13. content/accent/badge — [v1 리뷰 19, 73](../v1.md)에서 시맨틱 토큰에 컴포넌트명이 들어갔다고 올린 항목이 v03에서도 그대로입니다. favorite이 형제로 생긴 걸 용도별 축으로 읽고 코드도 이름을 따라갔습니다 → badge도 컴포넌트명이 아니라 용도명(예: highlight)이 맞지 않는지
- [ ] 14. accent 하위 축 기준 — surface/accent는 색상으로(orange, orange-subtle), content/accent는 용도로(badge, favorite) 갈립니다 → 같은 accent 아래 기준이 둘이라 쓸 때 헷갈립니다
- [ ] 15. content/rank의 위치 — 등급은 content 아래인데 같은 성격의 상승·하락은 trend로 최상위 그룹입니다 → 도메인 색을 최상위로 뺄지 content 아래로 모을지

## 코드가 이미 맞춘 것

- 리네임 3건 반영 — surface/accent/orange, surface/accent/orange-subtle, content/accent/badge. 사용처 4개 컴포넌트를 전부 교체했고 값이 같아 화면 변화는 없습니다
- 신설 6건 등록 — content/accent/favorite, content/error, content/rank의 king·expert·rookie·sprout
- [badge/reporter-rank](https://www.figma.com/design/WfW1Nkx1oiOWBHNwrw48IL/Design-Library?node-id=671-9967) 4등급 글자 색을 새 content/rank 토큰으로 교체. expert와 king은 심볼과 같은 색이 되면서 글자가 밝아졌습니다 (위 2번)
- 코드에 직접 적혀 있던 빨강 3곳을 토큰으로 교체 — 온보딩 오류 문구·오류 테두리는 content/error, 가게 상세 찜 하트는 content/accent/favorite. 값은 셋 다 그대로입니다

## 이전 문서에서 이어받은 항목

- 해소됨: [v1 73](../v1.md) 토큰 이름 3건 — v03이 같은 이름을 유지한 채 favorite을 형제로 추가해서 의도적 구조로 판단하고 코드를 Figma 이름에 맞췄습니다
- 그대로: [v1 19](../v1.md) content/accent/badge의 이름 자체 — 위 13번으로 다시 올립니다
- 그대로: [v1 94](../v1.md) radius/2xl 20px 미사용 — v03에서도 변화 없습니다

결과 확인은 배포된 /playground 파운데이션 Color에서 하시면 됩니다.
