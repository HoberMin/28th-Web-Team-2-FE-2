# 프론트 → 백엔드 요청 (2026-08-21)

> 프론트에서 `main`(`#222 KAMIS 도매가격 백필` 기준)을 클론해 검토했습니다.
> **백엔드 레포에는 커밋·푸시하지 않았습니다.** 로컬에서 고쳐 테스트만 돌려보고 diff로 드립니다.

## 요청 목록

| # | 페이지 | 메서드 | URI | 요약 |
|---|---|---|---|---|
| 1 | [`/onboarding`](https://marketgo.kro.kr/onboarding) 동네 선택 | GET | `/api/v1/regions/search` | 🔴 **후보가 여러 건인 검색어가 전부 502.** 원인 확정 + 패치 첨부 |
| 2 | [`/`](https://marketgo.kro.kr/) 홈 뉴스 카드 | GET | `/api/v1/news` | 🟡 503 `NEWS_UNAVAILABLE` — 수집 미실행인지 파싱 실패인지 |
| 3 | [`/onboarding`](https://marketgo.kro.kr/onboarding) 동네 선택 | GET | `/api/v1/regions/nearby` | 🟡 `regionId`가 **숫자**로 옵니다. 다른 곳은 문자열 |
| 4 | [`/onboarding`](https://marketgo.kro.kr/onboarding) 동네 선택 | GET | `/api/v1/regions/nearby` | 🟡 **좌표를 안 내려줍니다.** `search`는 줍니다 |

**1번만 먼저** 봐주셔도 됩니다. 나머지는 급하지 않습니다.

---

## 1. 🔴 `/api/v1/regions/search` — 흔한 검색어가 전부 502

온보딩에서 **동네를 고를 수 없습니다.**

```
GET /api/v1/regions/search?keyword=서초
→ 502 {"code":"EXTERNAL_API_ERROR","message":"외부 API 호출 중 오류가 발생했습니다."}
```

### 재현 (라이브 실측, 2026-08-21)

| keyword | 결과 |
|---|---|
| `신촌`(1건) · `판교`(2건) | 200 ✅ 좌표까지 정상 |
| `삼성` · `홍대` · `123` (0건) | 200 ✅ 빈 배열 |
| **`서초` `역삼` `대치` `삼성동`** | **502** ❌ |
| **`강남` `강남구` `종로` `부산` `제주`** | **502** ❌ |
| `동` · `가` · `abc` | 400 `INVALID_PARAMETER_ERROR` (정상) |

**후보가 여러 건 잡히는 검색어만 실패**합니다. 0건이거나 1~2건이면 통과합니다.

### 원인

`external/kakao-client/.../feign/KakaoResponseDecoder.java`

```java
private void validateDocuments(final JsonNode documents, final Type type) {
    documents.forEach(document -> validateDocument(document, type));   // 배열 전체를 엄격 검증
}

private void validateAddress(final JsonNode address) {
    final JsonNode detail = address.get("address");
    if (detail == null || !detail.isObject()) {
        throw externalApiException();                    // :135  502
    }
    requireNonBlankText(detail, "region_3depth_name");   // :141  502
}
```

카카오 `/v2/local/search/address.json`은 문서마다 `address`(지번)와 `road_address`(도로명)를 주는데
**둘 중 하나가 `null`일 수 있습니다.** 도로명으로만 매칭된 결과는 `address: null`이고, 시·구 단위
결과는 `region_3depth_name`이 빈 문자열입니다.

**둘 다 카카오의 정상 응답인데 외부 API 장애로 오인**합니다. 게다가 배열 전체를 검사해서
**`SEARCH_SIZE = 30`건 중 한 건만 어긋나도 응답 전체가 502**가 됩니다. 검색어가 흔할수록 이런
문서가 섞일 확률이 올라가는 것이 위 재현표의 패턴입니다.

> **가설 표시**: "서초·역삼은 도로명 전용 항목 때문", "종로·부산은 `region_3depth_name`이 빈 값
> 때문"이라는 **건별 귀속은 가설**입니다(API 키가 없어 카카오 원본 응답을 직접 보지 못했습니다).
> **디코더가 두 경우를 502로 만든다는 것과, 한 건이 전체를 죽인다는 것은 코드로 확정**입니다.

### 이미 있는 방어가 무력화돼 있습니다

`KakaoRegionSearchAdapter`에는 이상 항목을 건너뛰는 방어가 이미 있습니다.

```java
final String bCode = address.bCode();
if (bCode.isBlank()) {
    return;                    // 쓸 수 없는 항목은 건너뛴다 — 의도가 맞습니다
}
```

그런데 **디코더가 그 앞에서 죽어** 여기까지 도달하지 못합니다. 어댑터의 의도(걸러 쓴다)와
디코더의 동작(하나라도 이상하면 전부 버린다)이 어긋난 상태입니다.

`KakaoResponseDecoderTest`(143줄)에 **`address: null` 케이스가 없어** 테스트에 안 잡혔습니다.
`KakaoRegionSearchAdapterTest`는 `bCode` 빈 문자열을 커버하지만 **디코더를 통과한 뒤**의
이야기라 이 결함을 잡지 못합니다.

### 제안하는 수정

**"카카오의 정상 형태 차이"만 건너뛰고, 진짜 이상은 그대로 502**로 둡니다.

- `address`가 없거나 `region_3depth_name`이 빈 문서 → 법정동 정보가 없으므로 **그 문서만 버림**
- 좌표 범위 이상 · 필수 필드 누락 · 빈 객체(`{}`) → **기존대로 502**
- 응답 구조 자체가 깨진 경우 → `validateEnvelope`가 **기존대로 502**
- **주소 검색에만 적용.** `coord2regioncode`와 장소 검색은 결과 형태가 균일해 필수 필드 누락이
  곧 이상 신호이므로 건드리지 않았습니다

### 검증 결과

```
./gradlew :external:kakao-client:test :api:test
BUILD SUCCESSFUL — 692건 통과 · 실패 0 · 스킵 4
```

회귀 테스트 3건을 추가했습니다(아래 diff 포함).

- `도로명으로만_매칭돼_address가_null인_문서는_버리고_나머지를_반환한다`
- `동이_없는_시_구_단위_문서는_버리고_나머지를_반환한다`
- `응답_구조_자체가_깨지면_여전히_502로_막는다`

기존 `KakaoAddressSearchContractTest`의 502 계약(좌표 누락·범위 초과·빈 문서)은 **그대로 통과**합니다.
처음에 완화를 넓게 잡았다가 이 계약들이 깨져서, 형태 차이에만 적용하도록 좁혔습니다.

> **판단 하나 확인 부탁드립니다**: 이 수정은 30건 중 쓸 수 있는 게 하나도 없으면 **빈 배열 200**을
> 돌려줍니다(예전엔 502). 프론트는 "검색 결과 없음"으로 표시하면 되니 이쪽이 낫다고 봤는데,
> BE 관점에서 다르게 보신다면 알려주세요.

### 패치 (`git apply` 가능)

<details>
<summary>펼치기 — KakaoResponseDecoder.java + 회귀 테스트</summary>

```diff
diff --git a/external/kakao-client/src/main/java/com/example/demo/external/kakao/feign/KakaoResponseDecoder.java b/external/kakao-client/src/main/java/com/example/demo/external/kakao/feign/KakaoResponseDecoder.java
index 543a239..5a0dbc0 100644
--- a/external/kakao-client/src/main/java/com/example/demo/external/kakao/feign/KakaoResponseDecoder.java
+++ b/external/kakao-client/src/main/java/com/example/demo/external/kakao/feign/KakaoResponseDecoder.java
@@ -52,8 +52,7 @@ public final class KakaoResponseDecoder implements Decoder {
 
     private ObjectNode normalize(final JsonNode root, final Type type) {
         validateEnvelope(root, type);
-        final JsonNode documents = root.get(DOCUMENTS);
-        validateDocuments(documents, type);
+        final JsonNode documents = usableDocuments(root.get(DOCUMENTS), type);
         final ObjectNode normalized = root.deepCopy();
         final JsonNode meta = root.get(META);
         normalized.set(TOTAL_COUNT_CAMEL_CASE, meta.get(TOTAL_COUNT));
@@ -101,8 +100,59 @@ public final class KakaoResponseDecoder implements Decoder {
         }
     }
 
-    private void validateDocuments(final JsonNode documents, final Type type) {
-        documents.forEach(document -> validateDocument(document, type));
+    /**
+     * 카카오 주소 검색은 한 응답 안에 <b>법정동 정보가 없는 문서를 정상적으로</b> 섞어 내려준다.
+     * 도로명으로만 매칭돼 {@code address}가 null인 결과와, 동이 없어
+     * {@code region_3depth_name}이 빈 문자열인 시·구 단위 결과가 그렇다.
+     * 이는 외부 API 장애가 아니므로 해당 문서만 버리고 나머지는 그대로 쓴다.
+     *
+     * <p>예전에는 배열 전체를 엄격 검증해 한 건만 어긋나도 502를 던졌다. 그 결과
+     * 후보가 많은 검색어("서초"·"역삼"·"강남" 등)가 통째로 실패했다.
+     *
+     * <p>완화는 <b>형태 차이에만</b> 적용한다. 남은 문서의 좌표 범위·필수 필드는 예전처럼
+     * 엄격히 검증해 이상이면 502를 던지고, 응답 구조 자체가 깨진 경우는
+     * {@link #validateEnvelope}가 막는다. 좌표→행정구역과 장소 검색은 결과 형태가 균일해
+     * 필수 필드 누락이 곧 이상 신호이므로 완화 대상이 아니다.
+     */
+    private JsonNode usableDocuments(final JsonNode documents, final Type type) {
+        if (type != KakaoAddressSearchResult.class) {
+            documents.forEach(document -> validateDocument(document, type));
+            return documents;
+        }
+        final var usable = objectMapper.createArrayNode();
+        documents.forEach(document -> {
+            if (!carriesLegalRegion(document)) {
+                return;
+            }
+            validateDocument(document, type);
+            usable.add(document);
+        });
+        return usable;
+    }
+
+    /**
+     * 법정동(지번) 정보를 담은 문서인지. 담고 있지 않은 것은 오류가 아니라 "쓸 수 없음"이다.
+     *
+     * <p>판정 대상은 <b>형태를 갖춘 문서</b>뿐이다. 최상위 필수 필드조차 없는 문서(빈 객체 등)는
+     * 카카오의 정상 변형이 아니라 이상 응답이므로 {@code true}를 돌려 엄격 검증(502)으로 넘긴다.
+     */
+    private boolean carriesLegalRegion(final JsonNode document) {
+        if (document == null || !document.isObject()) {
+            return true;
+        }
+        if (!isNonBlankText(document, "address_name") || !isNonBlankText(document, "address_type")) {
+            return true;
+        }
+        final JsonNode detail = document.get("address");
+        if (detail == null || !detail.isObject()) {
+            return false;
+        }
+        return isNonBlankText(detail, "region_3depth_name");
+    }
+
+    private boolean isNonBlankText(final JsonNode document, final String field) {
+        final JsonNode value = document.get(field);
+        return value != null && value.isTextual() && !value.textValue().isBlank();
     }
 
     private void validateDocument(final JsonNode document, final Type type) {
diff --git a/external/kakao-client/src/test/java/com/example/demo/external/kakao/feign/KakaoResponseDecoderTest.java b/external/kakao-client/src/test/java/com/example/demo/external/kakao/feign/KakaoResponseDecoderTest.java
index 2310ee2..2bdc7f1 100644
--- a/external/kakao-client/src/test/java/com/example/demo/external/kakao/feign/KakaoResponseDecoderTest.java
+++ b/external/kakao-client/src/test/java/com/example/demo/external/kakao/feign/KakaoResponseDecoderTest.java
@@ -5,6 +5,7 @@ import static org.assertj.core.api.Assertions.assertThatThrownBy;
 
 import com.example.demo.common.exception.ApiException;
 import com.example.demo.common.exception.ErrorType;
+import com.example.demo.external.kakao.KakaoAddressSearchResult;
 import com.example.demo.external.kakao.KakaoCategorySearchResult;
 import com.example.demo.external.kakao.KakaoRegionCodeResult;
 import com.fasterxml.jackson.databind.ObjectMapper;
@@ -120,6 +121,51 @@ class KakaoResponseDecoderTest {
                 .isInstanceOfSatisfying(ApiException.class, this::assertExternalApiException);
     }
 
+    @Test
+    void 도로명으로만_매칭돼_address가_null인_문서는_버리고_나머지를_반환한다() throws Exception {
+        final var result = decode(
+                "{\"meta\":{\"total_count\":2},\"documents\":["
+                        + "{\"address_name\":\"서울 서초구 반포대로 58\",\"address_type\":\"ROAD_ADDR\","
+                        + "\"x\":\"127.0100\",\"y\":\"37.4900\",\"address\":null,"
+                        + "\"road_address\":{\"address_name\":\"서울 서초구 반포대로 58\"}},"
+                        + "{\"address_name\":\"서울 서초구 서초동\",\"address_type\":\"REGION\","
+                        + "\"x\":\"127.0276\",\"y\":\"37.4979\","
+                        + "\"address\":{\"address_name\":\"서울 서초구 서초동\","
+                        + "\"region_1depth_name\":\"서울\",\"region_2depth_name\":\"서초구\","
+                        + "\"region_3depth_name\":\"서초동\",\"b_code\":\"1165010800\"}}]}",
+                KakaoAddressSearchResult.class);
+
+        assertThat(result.addresses()).singleElement().satisfies(address ->
+                assertThat(address.address().bCode()).isEqualTo("1165010800"));
+    }
+
+    @Test
+    void 동이_없는_시_구_단위_문서는_버리고_나머지를_반환한다() throws Exception {
+        final var result = decode(
+                "{\"meta\":{\"total_count\":2},\"documents\":["
+                        + "{\"address_name\":\"서울 종로구\",\"address_type\":\"REGION\","
+                        + "\"x\":\"126.9790\",\"y\":\"37.5735\","
+                        + "\"address\":{\"address_name\":\"서울 종로구\","
+                        + "\"region_1depth_name\":\"서울\",\"region_2depth_name\":\"종로구\","
+                        + "\"region_3depth_name\":\"\",\"b_code\":\"1111000000\"}},"
+                        + "{\"address_name\":\"서울 종로구 청운동\",\"address_type\":\"REGION\","
+                        + "\"x\":\"126.9707\",\"y\":\"37.5874\","
+                        + "\"address\":{\"address_name\":\"서울 종로구 청운동\","
+                        + "\"region_1depth_name\":\"서울\",\"region_2depth_name\":\"종로구\","
+                        + "\"region_3depth_name\":\"청운동\",\"b_code\":\"1111010100\"}}]}",
+                KakaoAddressSearchResult.class);
+
+        assertThat(result.addresses()).singleElement().satisfies(address ->
+                assertThat(address.address().region3DepthName()).isEqualTo("청운동"));
+    }
+
+    @Test
+    void 응답_구조_자체가_깨지면_여전히_502로_막는다() {
+        assertThatThrownBy(() -> decode(
+                        "{\"documents\":[]}", KakaoAddressSearchResult.class))
+                .isInstanceOfSatisfying(ApiException.class, this::assertExternalApiException);
+    }
+
     private void assertExternalApiException(final ApiException exception) {
         assertThat(exception.errorType())
                 .isEqualTo(ErrorType.EXTERNAL_API_ERROR);
```

</details>

---

## 2. 🟡 `/api/v1/news` — 503

```
→ 503 {"code":"NEWS_UNAVAILABLE","message":"조회할 뉴스 데이터가 없습니다."}
```

`#218 네이버 뉴스 HTML 파싱 보정`·`#214 뉴스 데이터 없음 메시지 명확화`가 오늘 들어간 걸 보면
인지하고 계신 것 같습니다. **수집이 아직 안 돈 것인지, 파싱이 여전히 실패하는 것인지**
한 줄만 주시면 프론트에서 홈 뉴스 카드 처리를 정합니다.

---

## 3. 🟡 `regionId` 타입이 엔드포인트마다 다릅니다

| 엔드포인트 | `regionId` |
|---|---|
| `/regions/search` | `"1141011400"` **문자열** |
| `/regions/{id}/items/{id}/reports` | `"1168010600"` **문자열** |
| **`/regions/nearby`** | `1168010600` **숫자** |

법정동 코드는 **앞자리 0이 유실되면 안 되는 코드값**입니다(예: `0111010100`).
지금은 프론트에서 10자리로 `padStart`해 복원하는데, **자릿수 고정을 가정한 우회**라 불안합니다.
`/regions/nearby`도 문자열로 통일해 주실 수 있을까요?

---

## 4. 🟡 `/regions/nearby`만 좌표를 안 내려줍니다

```
/regions/search → {"regionId":"...", "regionName":"...", "latitude":37.56, "longitude":126.93}
/regions/nearby → {"regionId":1168010600, "regionName":"강남구 대치동"}          ← 좌표 없음
```

같은 "지역"인데 응답 모양이 다릅니다. 프론트는 좌표가 필요해서 `nearby`로 고른 지역은
**이름으로 `search`를 다시 호출해 좌표를 복구**하고 있었습니다.
**1번 502를 실제로 밟은 경로가 이것**입니다 — 이 비대칭이 1번의 폭발 반경을 키웠습니다.

`nearby`도 좌표를 함께 주시면 이 재검색이 통째로 사라집니다.
그때까지는 프론트에서 조회에 쓴 사용자 좌표를 얹어 임시 우회해 뒀습니다(BE가 좌표를 주면 그쪽이 우선).

---

## 참고 1 — 스웨거 36개 오퍼레이션 전수 대조 (2026-08-21)

`/v3/api-docs`의 **전 메서드**를 프론트 연결 상태와 대조했습니다.
(`/api/samples` 2건은 샘플이라 제외 — 실질 34건)

### 프론트가 새로 붙인 것 — 8건 (2026-08-21)

| 화면 | 메서드 | URI | 상태 |
|---|---|---|---|
| 시세 상세 | GET | `/api/v1/items/{itemId}/public-prices` | ✅ 화면 연결 (적재 전이라 비면 더미) |
| 시세 상세 | GET | `/api/v1/regions/{regionId}/items/{itemId}/reports` | ✅ 화면 연결 (비면 더미) |
| 가게 상세 | GET | `/api/v1/stores/{storeId}` | ✅ 화면 연결 — 쿼리로 실어 나르던 프로필을 직접 조회 |
| 마이페이지 | GET | `/api/v1/users/me/reports` | ✅ API 레이어 (화면은 시안 대기) |
| 마이페이지 | GET | `/api/v1/users/me/reports/weekly` | ✅ API 레이어 |
| 마이페이지 | PATCH | `/api/v1/users/me/reports/{reportId}` | ✅ API 레이어 |
| 마이페이지 | DELETE | `/api/v1/users/me/reports/{reportId}` | ✅ API 레이어 |
| 제보 | POST | `/api/v1/user-reports/image-analysis` | ✅ API 레이어 |

**DB 적재 전 구간은 더미를 지우지 않고 폴백으로 남겼습니다** — 상류가 정상 200으로 빈 배열을
주면 더미로 떨어집니다. 적재가 끝나면 폴백 분기만 걷어냅니다.

### 아직 못 붙인 것 — 1건

| 화면 | 메서드 | URI | 막힌 이유 |
|---|---|---|---|
| 시세 상세 | GET | `/api/v1/items/{itemId}/online-prices` | **`channelName` 실제 값을 몰라서** |

프론트는 쇼핑몰 로고를 컬리·오아시스·GS SHOP·11번가 4종으로 고정해 두고 있습니다.
응답이 지금 `[]`라 **`channelName`에 어떤 값이 오는지 확인할 수 없어** 로고 매핑을 못 짭니다.

**요청**: 수집이 붙으면 `channelName`·`channelKind`가 실제로 어떤 값인지(위 4종인지, 더 있는지)
알려주세요. 4종 밖이 있으면 디자이너에게 로고를 요청해야 합니다.

### 이미 붙어 있는 것 — 25건

인증(로그인·재발급·로그아웃) · 품목 목록/상세/찜 · 품목 제보 · 뉴스 · 지역(nearby·search·최저가) ·
가게(주변·추천·제보·단골) · 사용자(me·닉네임·관심지역·단골가게) · 이미지 업로드 · KAMIS 일별가격.

### 라이브 호출 결과 (공개 GET 13건)

`/api/v1/regions/search` **502**(§1)와 `/api/v1/news` **503**(§2) 외에는 **전부 200**입니다.
`/stores/recommendation`은 200이지만 결과 0건, `/items/{id}/online-prices`·`public-prices`도
200이지만 `[]`·`points: []`라 아직 데이터가 없는 것으로 보입니다.

### 동종 결함 점검

§1과 같은 **"한 건 어긋나면 전체 502"** 구조가 `validatePlace`(장소 검색)와
`validateRegion`(좌표→행정구역)에도 있습니다. 다만 **지금은 문제가 안 됩니다** — 좌표→행정구역은
결과가 1~2건이고 형태가 균일하며, 장소 검색의 `phone`·`road_address_name`은 `requireText`라
빈 문자열을 통과시킵니다(`requireNonBlankText`가 아닙니다). **지금 고칠 결함은 아니라고 봅니다.**
다만 카카오가 형태를 바꾸면 같은 방식으로 터질 자리라 적어 둡니다.

---

## 참고 3 — 프론트가 이미 처리한 것 (요청 아님)

| 페이지 | 메서드 | URI | 조치 |
|---|---|---|---|
| `/onboarding` | GET | `/api/v1/regions/nearby` | 조회에 쓴 사용자 좌표를 후보에 얹어 **재검색 자체가 안 일어나게** 함 → §1 우회 |
| `/onboarding` | POST | `/api/regions/selection` (우리 BFF) | 실패 원인을 삼키던 `catch`에 로그 추가 |
| `/prices/{itemId}` | GET | public-prices · 동네 제보 | 실 API 연결 + 비면 더미 폴백 |
| `/stores/{storeId}` | GET | `/api/v1/stores/{storeId}` | 프로필을 직접 조회로 전환 |

§4가 해결되면 첫 줄은 제거합니다(BE 좌표가 우선하도록 짜 뒀습니다).

---

## 부탁드리는 것

1. **§1 패치 검토** — 첨부 diff 그대로 쓰셔도 되고 방향만 취하셔도 됩니다.
   지금 온보딩이 막혀 있어 **이것만 먼저** 부탁드립니다.
2. §2 뉴스 상태 한 줄
3. §3 `regionId` 문자열 통일 가능 여부
4. §4 `nearby` 좌표 포함 가능 여부
