# 백엔드 요청 — 디자인 QA 반영에 막힌 것 (2026-08-21)

> 작성: 프론트(Next.js)
> 기준: Figma `화면GUI(원본)` 최종본 실측 + `https://api.marketgo.kro.kr/v3/api-docs` 실조회(2026-08-21)

**요청은 3건입니다. 전부 개발이 필요합니다.**

| # | 항목 | 막고 있는 화면 |
|:--:|---|---|
| 1 | 「깨·견과류」 카테고리 추가 | F04-2 야채 카테고리 · F02 야채시세 |
| 2 | 품목 목록 정렬 2종 추가 | F02 야채시세 정렬 시트 |
| 3 | 「오늘 제보된 품목」 수 필드 | F03 가게상세 · F03 가게 시트 |

**여기서 뺀 것 (요청 아님 · 확인만 했습니다)**

- **제보 `unit` 제약** — 물어보려 했는데 [#216](https://github.com/YAPP-Github/28th-Web-Team-2-BE-2/pull/216)에서 이미 해결해 주셨고 CD도 끝난 걸 확인했습니다. 소스를 읽고 계약을 파악했습니다(아래 참고).
- **공공가격·온라인가 데이터 적재** — `be-전달-2026-08-21.md` §2·§6에 이미 요청돼 있습니다. 다만 **아직 적재 전**이라 확인만 적어 둡니다(아래 참고).

---

## 1. 품목 카테고리에 「깨·견과류」 추가

- **대상**: `GET /api/v1/items` 의 `category` enum
- **현재**: `ROOT_VEGETABLES` · `LEAFY_GREENS` · `FRUITING_VEGETABLES` · `PEPPERS` · `SEASONINGS` · `MUSHROOMS` · `FRUITS` (7종)
- **필요**: 위 7종 + **깨·견과류** (총 8종)

**왜 필요한가**
디자인 최종본의 카테고리 목록이 8종입니다. 지금 참깨·땅콩이 `SEASONINGS`(마늘·파·생강)에 들어가 있어 화면에서 잘못된 카테고리로 노출됩니다.

**요청**: enum 추가 + 해당 품목 재분류

---

## 2. 품목 목록 정렬 2종 추가

- **대상**: `GET /api/v1/items` 의 `sort` enum
- **현재**: `NAME_ASC` · `PRICE_ASC` · `PRICE_DESC`
- **필요**:
  - **시세보다 저렴한 순** (공공시세 대비 할인율)
  - **최근 제보순**

**왜 필요한가**
디자인의 정렬 시트가 「가나다순 / 시세보다 저렴한 순 / 최근 제보순」 3종입니다. 가나다순은 `NAME_ASC`로 되지만 나머지 2종이 없어 화면 라벨을 시안대로 바꿀 수 없습니다. 라벨만 바꾸면 실제 동작과 어긋나서 그대로 두고 있습니다.

---

## 3. 「오늘 제보된 품목」 수 필드 추가

- **대상**: `StoreDetailResponse` (`GET /api/v1/stores/{storeId}`)
- **현재**: `totalReportedItemCount` (누적)
- **필요**: 오늘 기준 제보 품목 수 (예: `todayReportedItemCount`)

**왜 필요한가**
가게 상세와 지도 가게 시트의 배지 2개 중 하나가 「오늘 제보된 품목 N」입니다. 누적값으로는 계산할 수 없습니다.

**참고**: 같은 화면의 영업상태 · 오늘 영업시간 · 도보시간 · 저렴한 야채 수는 이미 응답에 있어서 **프론트에서 붙이면 됩니다.** 이 필드만 없습니다.

---

## 참고 A — 제보 `unit` 계약 (확인 완료, 요청 아님)

`CreateUserReportUseCase.normalizeQuantity`를 읽고 파악한 계약입니다. 프론트가 이해한 게 맞는지만 확인해 주시면 됩니다.

| 상황 | 결과 |
|---|---|
| 접두 수량 없는 표기(`kg`)를 기준 단위 `1kg` 품목에 보냄 | ✅ 허용 |
| 무게끼리 다른 단위(`1kg` 품목에 `500g`) | ✅ `amount=0.5` · `unit=1kg`으로 환산 저장 |
| 낱개끼리 다른 단위(`1개` 품목에 `포기`) | ❌ 400 |
| 무게 ↔ 낱개(`1kg` 품목에 `개`) | ❌ 400 |
| `2kg`에 `8000원` | ✅ `1kg에 4000원`으로 환산 저장 (`publicPriceDiff`도 환산 후 기준) |

**프론트가 이 계약에 맞춰 할 일** — 단위 선택 UI를 되살릴 때 **품목 `defaultUnit`과 같은 차원의 단위만** 선택지로 보여주면 됩니다(무게 품목은 kg·g, 낱개 품목은 그 단위만). 저희 쪽 작업이라 요청은 없습니다.

## 참고 B — KAMIS 적재 실측 (2026-08-21 15:1x)

`#222`·`#223`·`#230` 반영본이 배포된 뒤 다시 호출해 봤습니다. **아직 비어 있습니다.**

```
GET /api/v1/items?regionId=1168010600&size=3
→ baseDate: null · 46종 전부 price: null · priceGap: null

GET /api/v1/items/1/public-prices?period=WEEK&regionId=1168010600
→ points: []
```

백필 배치가 아직 안 돈 것으로 보입니다. 돌고 나면 알려주시면 프론트 더미 폴백을 걷어냅니다. (요청은 `be-전달-2026-08-21.md` §2에 있습니다 — 여기서 새로 드리는 요청은 없습니다.)
