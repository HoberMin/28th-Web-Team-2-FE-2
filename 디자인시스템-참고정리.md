# 디자인시스템 구축 참고 정리

다음 프로젝트(농산물 시세 서비스, 정보형 모바일)를 위해 토큰 기반 디자인시스템을 짓는다. 이 문서는 참고 사이트의 역할과 구축 순서, 협업 방식을 한 장으로 모은 것이다.

전제. 농산물 시세 서비스로 확정(2026년 7월, 청약에서 도메인 전환). 디자이너 2명, 팀 총 7명. 브랜드는 새로 만들되 이전 프로젝트의 구조(간격 타이포 짜임)는 재활용할 수 있으면 한다. 농산물 시세는 목록, 상세, 등락 표시 흐름이 핵심이다. 청약 때 있던 신청 폼, 약관 동의, 본인 인증 흐름은 이 서비스엔 없어서 뺀다.

---

## 1. 참고 사이트 전체와 차용할 부분

이 문서에서 다루는 레퍼런스를 한눈에 모았다. 각 사이트에서 차용하거나 참고할 부분만 적었다. 자세한 선정 이유와 GDS 컬러 차용 분석은 7번에 있다.

### 국내
| 사이트 | 차용하고 참고할 부분 |
| --- | --- |
| 여기어때 designlibrary.yeogi.com | 토큰 목차 짜는 법. Foundations 분류가 촘촘하다 |
| 당근 Seed seed-design.io | 토큰을 원시와 의미로 쪼개는 법. 컴포넌트 제작의 중심 참고처(오픈소스). AI 연결 |
| 쏘카 SOCAR Frame socarframe.socar.kr | 토큰과 다크모드를 만든 과정(기술 블로그 tech.socarcorp.kr). 짓는 과정을 보여줌 |
| 토스 TDS Mobile tossmini-docs.toss.im | 숫자, 가격 표시 방식 스팟 참고. 그대로 가져오지 않는다 |
| G마켓 GDS gds.gmarket.co.kr | 원시 팔레트와 역할 색. 브랜드와 CTA색 분리, 상태에 지연색, 이름으로 소통 |
| 채널톡 Bezier github.com/channel-io/bezier-react | 토큰이 실제 파일에서 어떻게 생겼는지 |
| getdesign.kr | 한국 서비스 브라우징 카탈로그 |
| 앱인토스 developers-apps-in-toss.toss.im | TDS Mobile로 대체. 안 봐도 된다 |

### 해외
| 사이트 | 차용하고 참고할 부분 |
| --- | --- |
| Material 3 m3.material.io | 원시와 역할 토큰 두 층 구조의 정석. 다크모드 토큰, on-color 짝, 타이포 역할 |
| Shopify Polaris polaris.shopify.com | 의미 토큰. 글쓰기 가이드(보이스 톤 오류 메시지). 기획에 유용 |
| GitHub Primer primer.style | 토큰 파일의 AI 설명 필드. AI 와이어프레임과 이어짐 |
| IBM Carbon carbondesignsystem.com | 토큰 주도 오픈소스. 데이터 밀도 높은 화면 |
| Uber Base Web baseweb.design | 테이블과 데이터 표시. 시세 목록과 상세 표 |
| Atlassian atlassian.design, Adobe Spectrum spectrum.adobe.com | 여유되면 비교용 |

정리하면 구조는 여기어때와 당근에서 배우고, 컴포넌트는 당근 Seed를 중심으로 직접 만든다. TDS Mobile은 숫자, 가격 표시 방식만 스팟 참고한다. 짓는 과정은 쏘카, 토큰 정석은 Material 3에서 본다.

---

## 2. 어디서 무엇을 볼지

정보형 모바일은 숫자와 목록이 빽빽하다. 사진 예쁜 서비스가 아니라 데이터를 잘 읽히게 하는 서비스다. 그래서 아래를 집중해서 본다.

- 타이포 스케일. 숫자(농산물 가격, 등락률, 시세 갱신 시각)를 몇 단계로 나눴나
- 레이아웃과 간격. 목록이 촘촘한데 안 답답하게 하는 규칙 (여기어때 Layout, 당근 spacing)
- 리스트와 행(Row) 컴포넌트. 시세 목록의 기본 뼈대 (TDS Mobile의 Table Row, List Row)
- 상태 표시(뱃지 태그). 상승, 하락, 보합 같은 시세 등락 색
- 색의 의미 구분. 상승은 빨강, 하락은 파랑, 보합은 회색 같은 규칙. 의미 토큰의 대표 사례

### 만들 시세 부품 목록 (당근 Seed 중심 참고, 직접 제작)

당근 Seed를 중심으로 직접 만든다. 아래는 기능 기준 체크리스트이고 이름은 예시다.

목록과 상세
- 목록. Table Row, Board Row, List Header, List Footer
- 상태 표시. Badge (상승, 하락, 보합)

지금부터 잡아둘 공통 뼈대
- Table Row 또는 List Row
- Badge
- BottomCTA

---

## 3. 구축 순서와 방법

컬러, 텍스트, radius, 컴포넌트 순서. 아래로 갈수록 위 층에 기댄다.

### 1) 컬러
- 원시 팔레트 먼저. 브랜드 색(오렌지), 회색 계열, 등락 색(상승 빨강, 하락 파랑). 초록 노랑은 지금 쓰임이 없어 보류
- 의미 토큰 얹기. bg/default, text/primary, text/secondary, border/subtle, state/danger 처럼 쓰임으로 묶기
- 라이트만 먼저. 다크는 수요를 나중에 판단

### 2) 텍스트(타이포)
- 스케일 단계 먼저. 큰 숫자, 제목, 본문, 보조, 캡션 정도로 5단계 안팎
- 원시(글자 크기, 줄 간격, 굵기) 위에 의미(title, body, caption, number)를 얹기
- 한글 폰트와 숫자 가독성을 여기서 결정. 농산물 시세는 가격과 등락률 숫자가 많으니 숫자 폭이 일정한 폰트가 유리

### 3) radius
- 4단계 안팎. none, sm, md, lg, 원형용 full
- 값만 만들지 말고 매핑 규칙도 적기. 카드는 md, 버튼은 sm, 뱃지는 full

### 4) 컴포넌트
- 앞의 세 토큰을 반드시 물려서 만들기. 색과 간격을 직접 박지 않기
- 자주 쓰는 것부터. 버튼, 리스트 행, 뱃지, 인풋, 하단 CTA
- 크기와 상태 같은 variant도 토큰으로

---

## 4. 협업 방식 (디자이너 2명, 팀 7명)

둘이 만들면 충돌 관리가 관건이다. 시작 전에 정한다.

- 원본 파일은 하나로. 토큰이 사는 피그마 파일을 하나만 두고 둘 다 거기를 본다. 복사본이 돌면 어긋난다
- 역할을 나눈다. 한 명이 Foundation(토큰), 한 명이 컴포넌트 담당. 원시 토큰은 한 사람이 확정하고 잠근다
- 네이밍 규칙 문서를 팀 전체와 공유한다. 토큰 이름이 나중에 코드에서 그대로 쓰인다. 디자이너 둘만의 약속이 아니라 팀 공용 문서다
- 리듬. 토큰을 잠그기 전에 둘이 같이 리뷰한다. 잠근 토큰을 바꾸면 물린 컴포넌트가 다 흔들린다

---

## 5. 아직 남은 결정거리

- 네이밍 규칙. 숫자 방식(blue-500)인지 뜻 방식(base). 여기가 흔들리면 뒤가 다 흔들린다. 시작할 때 시간을 쓴다
- 이전 프로젝트에서 재활용할 것. 간격 타이포 radius는 거의 그대로, 의미 토큰 짜임은 이름만, 브랜드 색 팔레트는 버리고 새로
- (해결됨) 도메인은 농산물 시세로 전환 확정(2026년 7월, 청약에서 전환). 목록, 상세, 등락 표시가 핵심 흐름

---

## 6. 다음 행동

1. 여기어때 Foundations로 내 토큰 목차의 큰 칸을 정한다
2. 당근 Seed에서 버튼 하나가 원시 토큰과 의미 토큰으로 어떻게 연결되는지 깊게 본다
3. TDS Mobile에서 뽑아둔 숫자, 가격 표시 방식을 내 서비스 체크리스트로 옮긴다
4. 디자이너 둘이 역할과 네이밍 규칙을 먼저 합의한다

---

## 7. 추가 참고처 (공개 디자인시스템)

### 국내 (실무 감각에 제일 도움)
- 쏘카 SOCAR Frame https://socarframe.socar.kr/ 그리고 기술 블로그 https://tech.socarcorp.kr/ 에서 토큰과 다크모드를 만든 과정을 글로 볼 수 있다. 짓는 과정을 보여주는 곳이라 구축 단계에 제일 맞다
- 당근 Seed https://seed-design.io/docs 토큰을 원시와 의미로 나눈 구조, AI 연결
- 채널톡 Bezier https://github.com/channel-io/bezier-react 오픈소스 공개. 토큰 JSON 39종, CSS 변수 450개. 토큰이 실제 파일에서 어떻게 생겼는지 볼 수 있다
- getdesign.kr https://getdesign.kr/ 한국 서비스 판 브라우징 카탈로그
- G마켓 GDS https://gds.gmarket.co.kr/foundation/color 공개 디자인시스템. 원시 팔레트와 역할 색을 실물로 볼 수 있다. 컬러 차용 분석은 아래에

### 해외 (토큰 구조 교과서)
- Material Design 3 https://m3.material.io/ 원시와 의미 토큰을 나눈 구조의 정석. 다크모드 토큰도 잘 보인다
- Shopify Polaris https://polaris.shopify.com/ 의미 토큰 226개. 글쓰기 가이드(보이스 톤 오류 메시지)가 최고. 기획에 유용
- GitHub Primer https://primer.style/ 토큰 파일에 AI용 설명 필드가 있다. AI 와이어프레임 목표와 이어짐
- IBM Carbon https://carbondesignsystem.com/ 토큰 주도 오픈소스. 데이터 밀도 높은 화면에 강함
- Uber Base Web https://baseweb.design/ 테이블과 데이터 표시가 강함. 시세 목록과 상세 표에 참고
- Atlassian https://atlassian.design/ , Adobe Spectrum https://spectrum.adobe.com/ 여유되면 비교용

### 가장 참고할 두 곳과 선정 이유

1. 쏘카 SOCAR Frame과 기술 블로그
- 국내 모바일 정보형 서비스라 농산물 시세와 화면 성격이 가깝다
- 토큰과 다크모드를 만든 과정을 한국어 글로 공개한 거의 유일한 곳이다. 완성물이 아니라 짓는 과정이 지금 우리에게 필요하다

2. Material 3
- 원시와 역할(의미) 토큰을 나눈 구조가 가장 교과서적이고 다크모드 토큰까지 명확하다
- 우리가 두 층 구조와 역할 기반으로 가기로 했는데 그 정석을 보여준다
- 피그마 커뮤니티 공개본이 있어 직접 뜯어볼 수 있다

### G마켓 GDS 컬러에서 차용할 부분 (파트너 공유용)

참고 링크. https://gds.gmarket.co.kr/foundation/color

G마켓 GDS는 공개 디자인시스템이라 원시 팔레트부터 역할 색까지 실물로 볼 수 있다. 우리가 짓는 두 층 구조(원시 더하기 역할)와 같은 방식이라 참고하기 좋다.

GDS 컬러 구조 요약
- 원시 팔레트와 역할 색(시멘틱)을 나눈다. 우리와 같은 방식
- 팔레트는 10단계(50부터 900), 500이 기준
- 브랜드는 Main Green 500(#00C400), 그런데 주요 버튼(CTA)은 파랑(#0028AC)으로 따로 둠
- 역할은 Text, Background, Border, State로 나눔
- Text는 위계가 여러 단계(primary, secondary, tertiary, caption)
- State는 Positive(초록), Informative(파랑), Warning(빨강), Delay(주황)
- hex 대신 컬러명으로 소통한다는 원칙

우리가 차용할 것 (근거와 함께)
1. 두 층 구조 유지. 근거. GDS도 원시 팔레트에 역할을 얹는다. 우리 방향이 맞다는 확인
2. 텍스트 위계를 여러 단계로. 근거. 농산물 시세는 정보 밀도가 높아 제목, 본문, 보조, 캡션의 구분이 필요하다. GDS의 text primary secondary tertiary caption 짜임을 참고해 우리 content 역할을 3에서 4단계로 둔다
3. 브랜드색과 CTA색을 분리. 근거. GDS는 브랜드가 초록이지만 주요 버튼은 파랑이다. 우리는 브랜드를 오렌지로 정했고 상승 하락 상태색은 빨강 파랑이라 겹치는 문제는 없지만, 브랜드색과 주요 버튼색을 따로 정할 수 있다는 발상 자체는 참고할 만하다
4. (재검토 필요) 상태색에 지연(Delay)을 추가하는 발상. 근거. GDS는 Positive Informative Warning에 더해 Delay(주황)를 둔다. 청약일 때는 이걸 예정과 대기 상태에 대응시켰는데, 도메인이 시세로 바뀌면서 상태 체계가 상승, 하락, 보합 3가지 등락(trend)으로 바뀌어 이 대응이 더는 그대로 성립하지 않는다. 넷째 상태가 실제로 필요해지면(예 데이터 지연 표시) 다시 볼 것
5. hex 아닌 이름으로 소통. 근거. 팀에서 색을 hex로 부르면 헷갈린다. GDS처럼 역할 이름으로 부르는 규칙을 팀 공용으로 삼는다

차용하지 않을 것
- 계열사 서비스 브랜드색(Smile 등). 농산물 시세엔 불필요
- GDS 고유 용어(text-cto 같은 이름)를 그대로 쓰지 않는다. 역할의 뜻만 가져오고 이름은 우리 규칙(content/default)으로

표기 차이 주의
- GDS는 붙임표(text-primary, Green-500)를 쓴다. 우리는 피그마라 슬래시(content/default, gray/500)로 쓴다. 이름 철학은 가져오되 표기는 우리 규칙으로 바꾼다

---

## 8. 컴포넌트 목록과 Foundation 구성 (이번주 실행)

원칙. 와이어프레임에 실제로 나올 화면에 필요한 것만 뼈대로 만든다. 전부 미리 만들지 않는다. "지금 꼭"부터 만들고 나머지는 화면 그리다 필요할 때 추가한다.

### 만들 컴포넌트

지금 꼭 (시세 핵심)
- 버튼 (크기, primary secondary)
- 인풋 필드
- 리스트 행 (Row)
- 뱃지 태그 (상승, 하락, 보합)
- 하단 CTA
- 상단 앱바
- 하단 네비게이션

여유되면
- 셀렉트 드롭다운, 세그먼트 컨트롤
- 체크박스 라디오 스위치
- 카드, 검색바, 칩 필터
- 빈 상태, 로딩 스켈레톤
- 바텀시트 다이얼로그 토스트

### 피그마 파일 구성

두 파일로 나눈다.
- [라이브러리] 파일: 토큰(Variables)과 컴포넌트 원본. 라이브러리로 게시
- [와이어프레임] 파일: 실제 화면. 라이브러리를 가져다 씀

한 파일로 시작하면 페이지를 Foundation, Components, Wireframe으로 나눈다.

### Foundation 구성 (이번주 버전, 색은 회색만)

1. Primitive 컬렉션
- color: gray-0 부터 gray-900. 브랜드 색(오렌지)과 등락 색(빨강 파랑)은 이름 자리만
- space: 2 4 8 12 16 20 24 32 40
- radius: 0 4 8 12 999
- font-size: 11 12 14 16 18 20 24 28 32

2. Semantic 컬렉션 (light 모드 먼저)
- bg: default, subtle, elevated
- text: primary, secondary, disabled
- border: default, subtle
- trend: 지금은 회색 임시, 색은 GUI 때
- 각 의미 값은 Primitive를 가리키게 연결. 다크 모드는 나중에 모드만 추가

3. 타이포 (Text Style)
- title-lg, title, body, body-strong, caption, number-lg
- 크기와 줄 간격은 font-size 변수 참조

### 시작 전 못 박을 결정
- 네이밍: 숫자 방식(gray-500) 대 뜻 방식(gray-strong). 숫자 방식 권장. 개발자 공유가 쉽고 단계 추가가 편하다

---

## 9. Foundation 값 상세 제안과 근거 (구축용)

농산물 시세 모바일 하나에 디자이너 2명, 팀 7명 규모에 맞춰 가볍게 잡았다. 각 값 뒤에 근거를 달았다. 근거는 세 종류다.
- 업계 표준. 안 바꿔도 되는 공통 관례
- 갈리는 관례. 팀이 정할 부분
- 시세 맞춤. 내 판단이라 검토가 필요한 부분

### 네이밍 규칙
- 원시 색: `색이름/명도` (gray/500, blue/500)
- 역할 색(시멘틱): `역할/변형` (surface/default, content/secondary)
- 숫자류(간격 radius 폰트): `종류/크기` (space/16, radius/md, font-size/16)
- 공통. 소문자 영어, 그룹은 슬래시로. 색만 두 층(원시 더하기 역할), 간격 radius 폰트는 원시 한 층. 숫자는 값을 그대로 이름에(space/16이 곧 16px)

근거
- 두 층 구조와 슬래시 그룹은 피그마 Variables 기본이자 Material, GDS, 쏘카, 당근 공통 (업계 표준)
- 값을 그대로 이름에 쓰는 건 Tailwind식 관례. 원티드는 반대로 추상 이름을 씀 (갈리는 관례, 값 방식 권장)
- 색만 두 층인 건 색은 다크모드 때문에 역할이 필요하고 간격은 원시로 충분해서 (실무 관례 더하기 규모 맞춤)

### 간격 스케일 (4 기반, 9단계)
```
space/2   = 2
space/4   = 4
space/8   = 8
space/12  = 12
space/16  = 16
space/20  = 20
space/24  = 24
space/32  = 32
space/40  = 40
```
주로 쓸 값은 8 12 16 (목록 행 안쪽 여백, 요소 사이).

근거
- 4 기반은 Material 4pt 8pt 그리드, iOS 등 거의 모든 모바일 공통 (업계 표준)
- 9단계로 줄인 건 농산물 시세 단일 앱 규모 맞춤 (규모 맞춤)

### radius 스케일 (6단계)
```
radius/none = 0
radius/sm   = 4     (뱃지, 작은 태그)
radius/md   = 8     (버튼, 인풋)
radius/lg   = 12    (카드)
radius/xl   = 16    (바텀시트)
radius/full = 999   (원형, 칩)
```
매핑 규칙을 같이 둔다. 카드는 lg, 버튼은 md.

근거
- sm md lg 네이밍은 Tailwind Chakra 관례 (업계 관례)
- 값(4 8 12 16)은 모바일 버튼 카드 관례
- 매핑을 문서로 남기는 건 쏘카 여기어때 방식에서 가져옴

### 폰트 크기 스케일 (9단계)
```
font-size/11 = 11   (캡션)
font-size/12 = 12
font-size/14 = 14   (보조 본문)
font-size/16 = 16   (기본 본문)
font-size/18 = 18
font-size/20 = 20   (섹션 제목)
font-size/24 = 24   (화면 제목, 강조 숫자)
font-size/28 = 28
font-size/32 = 32   (큰 숫자)
```

근거
- 11부터 32 배열은 iOS 안드로이드 타이포 관례. 11이 모바일 최소 가독 크기 (업계 관례)
- 큰 쪽을 넉넉히 둔 건 농산물 가격과 등락률 숫자 강조 (시세 맞춤, 검토 필요)

### 역할 구조 (시멘틱 색)
역할 구조는 색을 쓰임으로 묶은 체계다. gray/900(원시)을 화면에 직접 안 쓰고 content/default(역할)를 쓴다. 역할이 원시를 가리켜, 다크모드나 브랜드 교체가 자동으로 따라온다.
```
surface/default      기본 배경
surface/subtle       옅은 배경 (구역 구분)
surface/elevated     떠 있는 면 (바텀시트)

content/default      기본 글자
content/secondary    보조 글자
content/disabled     비활성
content/on-primary   브랜드색 위 글자

primary              주요 버튼과 링크
primary/pressed      눌린 상태

border/default       기본 선
border/strong        강한 선

trend/up             상승 (빨강)
trend/down           하락 (파랑)
trend/flat           보합 (회색)

state/danger         입력 오류
```
농산물 시세의 등락(상승 하락 보합)을 trend로 따로 둔 게 핵심. 이 서비스에서 색이 제일 많이 쓰이는 곳이다. 입력 오류는 등락과 성격이 달라 state로 따로 둔다.

근거
- surface content border 구조는 Material 3 color role, GDS의 Text Background Border State, 당근 role-based를 합침 (여러 시스템 공통)
- on-primary 같은 짝 개념은 Material 3의 on-color (특정 시스템 참고)
- trend up down flat은 농산물 시세 도메인 맞춤. 청약 때는 status open closed upcoming이었고 GDS State(Positive Informative Warning Delay)의 Delay를 예정 상태 근거로 썼는데, 도메인이 바뀌며 이 매핑은 더 이상 안 맞아 뺐다 (시세 맞춤, 검토 필요. 7번 GDS 분석 4번 항목 참고)

### 타이포 스케일 (역할 이름, 8개)
```
title/1    = 24, bold       화면 제목
title/2    = 20, bold       섹션 제목
title/3    = 18, semibold   소제목
body/1     = 16, regular    기본 본문
body/2     = 14, regular    보조 본문
label      = 14, medium     버튼, 입력 라벨
caption    = 12, regular    부가 설명
number/lg  = 24, bold       가격, 등락률 같은 강조 숫자
```

근거
- title body label caption 네이밍은 Material 타이포 역할(display headline title body label) 관례 (업계 관례)
- number/lg는 시세 가격, 등락률 강조를 위한 도메인 판단. 토스 TDS의 숫자 처리도 참고감 (시세 맞춤, 검토 필요)

### 팀이 정할 결정
- 이름을 값으로(space/16) 할지 단계로(space/4가 16px) 할지. 값 방식 권장 (갈리는 관례)
- 타이포 굵기 단계. regular medium semibold bold 4단계인데 팀 폰트가 다 지원하는지 확인
- 시세 맞춤 세 개를 집중 검토. 큰 폰트 넉넉히, trend의 up down flat, number 강조

---

## 10. 컴포넌트 네이밍 규칙

### 문제 상황

컴포넌트 섹션(node-id 186:3268)을 실제로 확인해보니 표기 방식이 컴포넌트마다 제각각이었다.

- 케밥케이스. `icon-chevron-right`, `icon-chevron-left`
- 스네이크케이스. `icon_store_fill`, `icon_map_pin_fill`, `story_item`
- 단어 구분자 누락. `pricestory_card`(price+story), `pricecarousel_item`(price+carousel)
- 대문자 약어. `GNB`, `CTA` — 나머지가 다 소문자인데 이 둘만 튐
- 이름 안의 단어 순서 불일치. `storecard_vegetable_list`는 유형(storecard)이 앞, `recommended_store`, `recommended_store_card`는 유형(store, card)이 뒤. 같은 성격의 컴포넌트인데 순서 기준이 없어 찾기 어려움
- 의미 불명 숫자. `shape_48`, `vegetable_item_48` — 48이 크기 variant인지 고정값인지 이름만으론 구분 안 됨
- 미완성 레이어. `만들어야함`이라는 텍스트가 컴포넌트 자리에 그대로 남아 있음

### 왜 이 규칙으로 정했는가

세 가지 표기(케밥, 스네이크, 파스칼) 중 하나를 새로 고르지 않고, **9번에서 이미 정한 토큰 규칙을 컴포넌트로 그대로 확장**하기로 했다.

- 이미 있는 규칙(소문자 영어, 그룹은 슬래시)을 재사용하면 팀이 규칙을 두 개 따로 외울 필요가 없다
- Figma는 컴포넌트 이름에 슬래시를 쓰면 컴포넌트 피커에서 슬래시 앞부분 기준으로 자동 폴더링된다. 순서 불일치 문제(`storecard_vegetable_list` vs `recommended_store`)가 이름 구조를 고정하는 것만으로 같이 해결됨
- `Variant=Primary, State=Normal` 방식(CTA 컴포넌트)은 이미 Figma 컴포넌트 속성 표준이라 그대로 유지. variant/state/size는 이름에 넣지 않고 속성으로 뺀다는 원칙도 여기서 같이 정리
- 파스칼케이스도 후보였지만, 대문자 표기가 매번 번거로워서 소문자로 통일하기로 함 (실제로 Figma 컴포넌트 섹션을 다시 확인해보니 파스칼케이스로 된 컴포넌트는 없었고, 케밥/스네이크/구분자 누락/전체대문자만 섞여 있었음)
- `/playground` 배포 페이지의 디자인 토큰(`radius/sm`, `background/primary` 등)이 이미 이 표기(소문자+하이픈+슬래시)를 쓰고 있어서, 컴포넌트도 같은 규칙을 따르면 Figma와 코드 사이 표기가 어긋나지 않음

### 규칙

- 소문자 영어. 단어 사이는 하이픈(`-`)
- 이름 구조는 `분류/세부이름`으로 고정. 분류가 항상 앞
- variant(상태·크기 등 값이 바뀌는 축)는 이름에 넣지 않고 컴포넌트 속성(`Variant=`, `Size=` 등)으로 분리
- 대문자 약어도 소문자로 통일

### 적용 예시

| 현재 | 변경 |
| --- | --- |
| `icon-chevron-right/left/up/down` | `icon/chevron-right`, `icon/chevron-left`, `icon/chevron-up`, `icon/chevron-down` |
| `icon_store_fill`, `icon_store_stroke` | `icon/store-fill`, `icon/store-stroke` |
| `icon_map_pin_fill`, `icon_search` | `icon/map-pin-fill`, `icon/search` |
| `story_item`, `story_list` | `card/story`, `list/story` |
| `pricestory_card` | `card/price-story` |
| `recommended_store`, `recommended_store_card` | `card/recommended-store` (용도가 겹치면 하나로 통합 검토) |
| `storecard_vegetable_list` | `list/storecard-vegetable` |
| `lowest_vegetable_list` | `list/lowest-vegetable` |
| `pricecarousel_item` | `carousel/price-item` |
| `vegetable_grid_item/trend/price` | `grid/vegetable-item`, `grid/vegetable-trend`, `grid/vegetable-price` |
| `GNB` | `nav/gnb` |
| `CTA` (버튼 프레임) | `button/cta` (Variant=, State= 속성은 그대로 유지) |
| `textfield` | `field/text` (Property 1=normal/typing 속성 유지) |
| `chip_filter` | `filter/chip` (Property 1=normal/selected 속성 유지) |
| `more_badge` | `badge/more` |
| `img_grass` | `image/grass` |
| `shape_48`, `vegetable_item_48` | 이름에서 숫자 제거하고 크기가 variant면 `Size=48` 속성으로 이동 |
| `만들어야함` | 삭제하거나 실제 컴포넌트로 완성 후 정식 이름 부여 |

### 남은 결정거리
- `recommended_store`와 `recommended_store_card`가 실제로 다른 용도인지, 하나가 미사용 잔여물인지 확인 필요
- 리네이밍은 디자이너 2명이 함께 보는 파일이므로 (4번 협업 방식 참고) 적용 전에 합의하고 한 번에 반영
